# 项目简历 - AI 万能视频下载总结器 (SaveAny)

## 一、项目概述

**项目名称**: AI 万能视频下载总结器 (SaveAny)
**项目周期**: 2周
**技术栈**: Vue 3 + FastAPI + yt-dlp + DeepSeek + Stripe
**项目地址**: https://github.com/liyupi/free-video-downloader
**个人职责**: 全栈独立开发

---

## 二、项目描述

基于 AI 编程实战开发的**全栈视频工具平台**，支持从 1800+ 平台下载视频并提供 AI 智能总结，帮助用户高效学习和内容创作。

**核心亮点**:
- 多平台视频解析与下载（YouTube、B站、抖音等）
- AI 视频内容摘要与思维导图生成
- 多模态侵权检测系统
- 高并发 API 服务架构

---

## 三、技术亮点 (可写进简历)

### 1. 多平台视频解析引擎
```
- 基于 yt-dlp 二次封装，支持 1800+ 网站
- 针对抖音等平台开发专用解析模块（无需 Cookie 获取无水印视频）
- 异步下载任务队列，支持断点续传
```

### 2. AI 视频处理流水线
```
- 视频字幕自动提取 (Whisper/yt-dlp)
- DeepSeek 大模型 API 集成，流式输出摘要
- SSE 实时推送，Markdown 格式渲染
- 思维导图自动生成 (Mermaid)
```

### 3. 高并发 API 服务
```
- FastAPI 异步框架 + Uvicorn 多 Worker 部署
- 滑动窗口限流 (Rate Limiter)
- LRU 内存缓存 + 可选 Redis 分布式缓存
- HTTP 连接池复用
- 性能监控 (P95/P99 延迟统计)
```

### 4. 多模态侵权检测系统
```
- 视频指纹生成 (pHash + dHash + SIFT + 小波哈希)
- 纹理特征提取 (LBP 局部二值模式)
- CLIP 向量数据库语义相似度搜索
- 动态阈值匹配算法
- 音频指纹比对 (MFCC 特征)
```

### 5. 支付与用户系统
```
- JWT 无状态认证
- Stripe 国际支付集成 + Webhook 异步回调
- 支付宝/微信支付集成
- 会员权限分级控制
```

---

## 四、核心模块代码能力

### 后端 (Python/FastAPI)

**异步任务处理**:
```python
# 异步视频下载，支持并发
async def download_video(url: str, format_id: str):
    loop = asyncio.get_event_loop()
    result = await loop.run_in_executor(None, downloader.download, url, format_id)
    return result
```

**多模态特征提取**:
```python
# 视频指纹生成
class VideoFingerprintGenerator:
    def extract_frames(self, video_path: str, num_frames: int = 16):
        # 均匀采样帧
        pass
    
    def generate_phash(self, frame: np.ndarray) -> str:
        # 感知哈希
        pass
    
    def extract_sift(self, frame: np.ndarray) -> List:
        # SIFT 特征点
        pass
```

**CLIP 向量搜索**:
```python
class CLIPVectorDB:
    def extract_video_embeddings(self, video_path: str) -> np.ndarray:
        # 提取 512 维 CLIP 特征
        pass
    
    def search_by_video(self, query: str, top_k: int = 5):
        # FAISS 向量相似度搜索
        pass
```

### 前端 (Vue 3)

**响应式布局 + 状态管理**:
```javascript
// Pinia 状态管理
const useVideoStore = defineStore('video', {
  state: () => ({ videos: [], currentVideo: null }),
  actions: { async parseVideo(url) { ... } }
})
```

**SSE 流式渲染**:
```javascript
// EventSource 流式接收 AI 摘要
const eventSource = new EventSource(`/api/summarize?url=${url}`)
eventSource.onmessage = (e) => { this.summary += e.data }
```

---

## 五、简历项目描述模板

### 简洁版 (推荐)

> **AI 万能视频下载总结器** | Vue 3 + FastAPI + DeepSeek
> - 基于 yt-dlp 开发多平台视频下载模块，支持 1800+ 网站
> - 集成 DeepSeek 大模型，实现 AI 视频摘要、流式输出、思维导图生成
> - 设计高并发 API 服务，集成限流、缓存、连接池等性能优化
> - 开发多模态侵权检测系统，结合 CLIP 向量搜索与传统指纹比对

### 详细版

> **AI 万能视频下载总结器** | 全栈独立开发 | 2周
>
> **项目背景**: 解决用户多平台视频下载痛点，提供 AI 智能总结提升学习效率
>
> **技术实现**:
> - 后端: FastAPI 异步框架 + yt-dlp 视频解析 + DeepSeek API 流式摘要
> - 前端: Vue 3 + Composition API + SSE 实时渲染 + Pinia 状态管理
> - 高并发: 多 Worker 部署 + 滑动窗口限流 + LRU 缓存 + HTTP 连接池
> - 版权检测: pHash/SIFT/LBP 多维指纹 + CLIP 向量语义搜索 + 动态阈值
>
> **个人成长**: 掌握了 AI 编程工作流、大模型 API 集成、高并发服务设计

---

## 六、面试可能问到的问题

| 问题 | 回答要点 |
|------|----------|
| 为什么用 FastAPI 而不是 Django/Flask？ | 异步原生支持、自动 OpenAPI 文档、性能接近 Node.js |
| SSE 和 WebSocket 区别？ | SSE 单向推送、实现简单、更适合 AI 流式输出 |
| 如何保证支付安全？ | Stripe Webhook 签名验证、幂等处理、事务补偿 |
| 高并发如何限流？ | 滑动窗口算法、Redis 计数器、令牌桶 |
| CLIP 向量搜索原理？ | 预训练视觉-语言模型、FAISS 近似最近邻 |
| 视频指纹如何处理旋转/缩放？ | 多尺度 pHash、几何哈希、内容描述符 |

---

## 七、GitHub README 项目描述

```
# AI 万能视频下载总结器 (SaveAny) ⭐️

基于 Vue 3 + FastAPI + DeepSeek 的 AI 视频工具，支持 1800+ 平台下载和 AI 智能总结。

## 核心功能

🎬 多平台下载 - YouTube、B站、抖音等 1800+ 网站
🤖 AI 视频摘要 - DeepSeek 大模型流式输出
🧠 思维导图 - 自动生成交互式导图
🔍 侵权检测 - 多模态视频指纹 + CLIP 向量搜索
💳 支付集成 - Stripe/支付宝/微信

## 技术栈

- 前端: Vue 3 + Vite + Pinia
- 后端: FastAPI + Uvicorn + yt-dlp
- AI: DeepSeek API + CLIP
- 数据库: SQLite + Redis
- 部署: Docker + Nginx

## 快速开始

pip install -r requirements.txt
python main.py
```

---

## 八、可延伸的简历项目

如果你想增加更多项目，可以基于现有代码扩展：

1. **视频批量处理器** - ffmpeg 批量转码/剪辑
2. **视频水印检测器** - 图像识别 + 去水印
3. **直播录制工具** - 斗鱼/虎牙/B站直播流捕获
4. **视频素材库** - 基于 CLIP 语义搜索的素材管理
5. **自动化剪辑助手** - AI 自动生成短视频精彩集锦

---

*模板版本: v2.0 | 更新日期: 2026-04-05*
